apply plugin: 'application'

mainClassName = 'org.restler.todo.client.TodoClient'

dependencies {
    compile 'org.restler:restler-core:0.3.1',
            'com.fasterxml.jackson.module:jackson-module-paranamer:2.6.1',
            project(':api')
}
